{{ ansible_managed | comment }}
# Env file for objectindex-api.service.
# Generated by Ansible — edits will be overwritten on the next playbook run.

OBJIDX_DATABASE_URL={{ objidx_database_url }}
OBJIDX_S3={{ objidx_s3 }}
OBJIDX_BUCKETS={{ objidx_buckets | join(',') if (objidx_buckets is iterable and objidx_buckets is not string) else objidx_buckets }}
HOST={{ objectindex_host }}
PORT={{ objectindex_port }}
WORKERS={{ objectindex_workers }}
