[defaults]
inventory = inventory/hosts.yml
roles_path = roles
# Lab/homelab convenience — drop or tighten for hardened environments.
host_key_checking = False
# This deploy is fully unprivileged; never escalate.
become = False
